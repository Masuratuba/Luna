# LUNA visual shell

The home screen visual is intentionally generated with HTML/CSS only.
No PNG, JPG, background image, or external image asset is required.

The header uses a CSS-rendered moon/orb, orbital rings, stars, glow, and the LUNA wordmark. The chat remains functional and responsive.
